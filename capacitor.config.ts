import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jronline.app',
  appName: 'Jhang Rang Online Store',
  webDir: 'build'
};

export default config;
